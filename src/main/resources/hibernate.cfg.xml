<?xml version="1.0" encoding="UTF-8"?>
<hibernate-configuration>
    <session-factory>

        <!-- Connexion MySQL -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL55Dialect</property>
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3307/db_hwd8293a_2</property>
        <property name="hibernate.connection.username">hwd8293a</property>
        <property name="hibernate.connection.password">hwd8293a</property>

        <!-- Logs SQL -->
        <property name="hibernate.show_sql">true</property>

        <!-- Sessions -->
        <property name="hibernate.current_session_context_class">thread</property>

        <!-- Mise à jour automatique -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- PAS DE MAPPING ICI — HibernateUtil le fait déjà -->
    </session-factory>
</hibernate-configuration>
